[pytest]
testpaths = tests
pythonpath = . app
addopts = -ra
markers =
    api: API-level tests (FastAPI TestClient)
    unit: Unit-level tests (CRUD, pure functions)
filterwarnings =
    ignore::DeprecationWarning


